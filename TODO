Session/Meteor
 * meteor.Connection is currently expecting Strings whereas Meteor.Session is
   sending and receiving atoms. This should be changed to allow for other 
   transmission encodings


TODO for 0.1
* NOTIFICATIOn
	* new message (beep)
	* sound on/off
	* I wonder if that can be confirgured...
* instructions
* resize layout for smaller interface
* ACCESSABILITY
	* accessability: use anchors for links.
	* name every icon: title="click here to do this"
* PERSISTENT CHAT
	* Jobtask feature build for div integration + User object
	* persistent chats using cookies
	* Interface integration re: collapse a div - like facebook.

